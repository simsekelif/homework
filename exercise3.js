// 3) Print out every third number between 1 and 100. Try to limit the number of iterations (it should be less than 100). 

for ( let i = 1; i <=100; i+=3 ) {
   
    if( i === 1) {
        continue;
    }
    console.log(i);
}

